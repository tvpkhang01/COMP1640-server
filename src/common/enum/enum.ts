export enum GenderEnum {
  MALE = 'male',
  FEMALE = 'female',
}

export enum RoleEnum {
  ADMIN = 'admin',
  STUDENT = 'student',
  MM = 'manager marketing',
  MC = 'manager cordinator',
  GUEST = 'guest',
}

export enum Order {
  ASC = 'ASC',
  DESC = 'DESC',
}
